"""Frame constraint solve — straighten + pin + conformal.

TODO: Core CFTUV pipeline:
1. Straighten H_FRAME segments (all verts to same V) and V_FRAME (same U)
2. Pin frame vertices
3. Run conformal solve
4. Unpin
"""
